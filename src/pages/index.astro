---
import BaseLayout from "../layouts/BaseLayout.astro";

const services = [
  {
    title: "אבחון והערכה",
    brief: "מיפוי מקצה לקצה של שרשרת האספקה לזיהוי פערים, צווארי בקבוק והזדמנויות לשיפור.",
    detail: "ניתוח מעמיק הכולל סקירת תהליכים, מדדי ביצוע, מערכות מידע וממשקים ארגוניים. התוצר: דו״ח ממצאים מפורט עם תעדוף המלצות לפי ROI.",
    icon: "diagnostic",
  },
  {
    title: "אסטרטגיה ומודל תפעולי",
    brief: "עיצוב אסטרטגיית שרשרת אספקה התומכת ביעדי הצמיחה העסקיים.",
    detail: "בניית מודל תפעולי (Operating Model) המתאים לגודל החברה, מורכבות המוצרים ודרישות השוק. כולל הגדרת KPIs ותוכנית יישום.",
    icon: "strategy",
  },
  {
    title: "תכנון רשת הפצה",
    brief: "אופטימיזציה של מיקומי מחסנים, נתיבי שינוע ורמות שירות.",
    detail: "מודלים כמותיים לתכנון רשת שמאזנים בין עלות לבין רמת שירות. ניתוח תרחישים (What-If) לתמיכה בהחלטות השקעה.",
    icon: "network",
  },
  {
    title: "אופטימיזציית מלאי",
    brief: "הפחתת הון חוזר תוך שמירה על זמינות גבוהה ללקוח.",
    detail: "יישום מתודולוגיות מתקדמות לסגמנטציית מלאי, קביעת מדיניות הזמנה אופטימלית וניהול מלאי ביטחון מבוסס נתונים.",
    icon: "inventory",
  },
  {
    title: "תכנון ביקושים (S&OP)",
    brief: "תהליך Sales & Operations Planning שמחבר בין מכירות, תפעול וכספים.",
    detail: "הקמת תהליך S&OP מובנה הכולל Demand Review, Supply Review וExecutive Review. שיפור דיוק התחזיות והפחתת אפקט השוט.",
    icon: "demand",
  },
  {
    title: "רכש ואסטרטגיית ספקים",
    brief: "מיקסום ערך מספקים באמצעות ניהול קטגוריות ומשא ומתן אסטרטגי.",
    detail: "פיתוח אסטרטגיית רכש לפי קטגוריות, הערכת ספקים, ניהול סיכונים בשרשרת האספקה ובניית הסכמי SLA.",
    icon: "procurement",
  },
  {
    title: "מצוינות תפעולית",
    brief: "שיפור מתמיד באמצעות Lean, Six Sigma וכלי ניהול איכות.",
    detail: "יישום מתודולוגיות שיפור תהליכים, סטנדרטיזציה של נהלי עבודה, והטמעת תרבות של שיפור מתמיד בארגון.",
    icon: "excellence",
  },
  {
    title: "טרנספורמציה דיגיטלית",
    brief: "בחירה והטמעה של פתרונות טכנולוגיים לשרשרת אספקה חכמה.",
    detail: "מיפוי צרכים, בחירת מערכות (ERP, WMS, TMS), ניהול פרויקטי הטמעה וליווי Change Management.",
    icon: "digital",
  },
  {
    title: "קיימות ו-ESG",
    brief: "שילוב עקרונות קיימות בשרשרת האספקה מבלי לפגוע ברווחיות.",
    detail: "מיפוי טביעת רגל פחמנית, אופטימיזציית אריזה ושינוע, רכש אחראי ודיווח ESG בהתאם לתקנים בינלאומיים.",
    icon: "sustainability",
  },
];

const methodology = [
  {
    step: "01",
    title: "גילוי",
    desc: "הבנת האתגר העסקי, מיפוי בעלי עניין והגדרת יעדים מדידים.",
  },
  {
    step: "02",
    title: "ניתוח",
    desc: "איסוף נתונים, ניתוח תהליכים וזיהוי שורשי הבעיה.",
  },
  {
    step: "03",
    title: "אסטרטגיה",
    desc: "בניית תוכנית פעולה מתועדפת עם Business Case לכל יוזמה.",
  },
  {
    step: "04",
    title: "יישום",
    desc: "ליווי צמוד ביישום, מדידת תוצאות והתאמות בזמן אמת.",
  },
];

const painPoints = [
  {
    title: "עלויות תפעול גבוהות",
    desc: "חוסר יעילות בשרשרת האספקה שוחק את הרווחיות ומגביל צמיחה.",
    icon: "cost",
  },
  {
    title: "חוסן מול שיבושים",
    desc: "שרשרת אספקה שברירית חושפת את הארגון לסיכונים ועיכובים.",
    icon: "resilience",
  },
  {
    title: "נראות וביקורת",
    desc: "היעדר שקיפות מקצה לקצה מונע קבלת החלטות מבוססת נתונים.",
    icon: "visibility",
  },
];
---

<BaseLayout title="יסמין פרס | יועצת שרשרת אספקה אסטרטגית">

  <!-- ═══════════════════════════════════════════════════
       SECTION 1: HERO
       ═══════════════════════════════════════════════════ -->
  <section id="home" class="relative min-h-[92vh] flex items-center bg-navy-500 overflow-hidden scroll-mt-20">
    <!-- Background pattern -->
    <div class="absolute inset-0 opacity-[0.04]" aria-hidden="true">
      <svg width="100%" height="100%" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <pattern id="grid" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="white" stroke-width="0.5"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid)" />
      </svg>
    </div>
    <!-- Gradient accent -->
    <div class="absolute top-0 left-0 w-1/2 h-full bg-gradient-to-r from-teal-500/5 to-transparent" aria-hidden="true"></div>

    <div class="relative max-w-6xl mx-auto px-6 py-20 w-full">
      <div class="grid md:grid-cols-5 gap-12 md:gap-16 items-center">
        <!-- Text (right side in RTL = first in DOM) -->
        <div class="md:col-span-3 space-y-8">
          <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-teal-500/10 border border-teal-500/20">
            <span class="w-1.5 h-1.5 rounded-full bg-teal-400 animate-pulse"></span>
            <span class="text-xs font-medium text-teal-400 tracking-wide">יועצת שרשרת אספקה אסטרטגית</span>
          </div>

          <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-white leading-[1.15] animate-fade-up">
            שרשרת האספקה שלכם
            <br />
            <span class="text-teal-400">יכולה להיות</span>
            <br />
            <span class="text-teal-400">יתרון תחרותי.</span>
          </h1>

          <p class="text-lg sm:text-xl text-navy-200 max-w-xl leading-relaxed animate-fade-up" style="animation-delay: 0.15s">
            אני עוזרת לחברות להפוך את שרשרת האספקה ממרכז עלות למנוע צמיחה — באמצעות אסטרטגיה מבוססת נתונים וניסיון של שנים בתעשייה.
          </p>

          <div class="flex flex-wrap gap-4 animate-fade-up" style="animation-delay: 0.3s">
            <a
              href="#contact"
              class="inline-flex items-center gap-2 px-8 py-4 bg-teal-500 text-navy-500 font-bold text-base rounded-lg hover:bg-teal-400 transition-all duration-200 hover:shadow-xl hover:shadow-teal-500/20 hover:-translate-y-0.5"
            >
              לתיאום פגישת היכרות
              <svg class="w-4 h-4 rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
            </a>
            <a
              href="#services"
              class="inline-flex items-center gap-2 px-8 py-4 border border-navy-300/30 text-navy-200 font-medium text-base rounded-lg hover:border-navy-200 hover:text-white transition-all duration-200"
            >
              לשירותים שלי
            </a>
          </div>

          <!-- Quick stats -->
          <div class="flex gap-10 pt-4 animate-fade-up" style="animation-delay: 0.45s">
            <div>
              <span class="block text-3xl font-extrabold text-white">15+</span>
              <span class="text-sm text-navy-300">שנות ניסיון</span>
            </div>
            <div class="border-r border-navy-400/30"></div>
            <div>
              <span class="block text-3xl font-extrabold text-white">50+</span>
              <span class="text-sm text-navy-300">פרויקטים</span>
            </div>
            <div class="border-r border-navy-400/30"></div>
            <div>
              <span class="block text-3xl font-extrabold text-white">CSCP</span>
              <span class="text-sm text-navy-300">הסמכת APICS</span>
            </div>
          </div>
        </div>

        <!-- Headshot placeholder (left side in RTL) -->
        <div class="md:col-span-2 flex justify-center animate-fade-in" style="animation-delay: 0.3s">
          <div class="relative w-72 h-96 sm:w-80 sm:h-[28rem]">
            <!-- Decorative frame -->
            <div class="absolute -inset-3 rounded-2xl border border-teal-500/20" aria-hidden="true"></div>
            <div class="absolute -inset-6 rounded-2xl border border-teal-500/10" aria-hidden="true"></div>
            <!-- Image container -->
            <div class="relative w-full h-full rounded-xl bg-navy-400/30 overflow-hidden flex items-end justify-center">
              <!-- Placeholder silhouette -->
              <div class="absolute inset-0 bg-gradient-to-t from-navy-500/80 via-transparent to-transparent z-10"></div>
              <svg class="w-48 h-72 text-navy-400/40" viewBox="0 0 200 300" fill="currentColor" aria-hidden="true">
                <ellipse cx="100" cy="80" rx="50" ry="55" />
                <path d="M30 300 Q30 180 100 170 Q170 180 170 300 Z" />
              </svg>
              <div class="absolute bottom-4 left-4 right-4 z-20 text-center">
                <span class="text-xs text-navy-300/60 italic">מקום לתמונה מקצועית</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom fade -->
    <div class="absolute bottom-0 left-0 right-0 h-24 bg-gradient-to-t from-white to-transparent"></div>
  </section>

  <!-- ═══════════════════════════════════════════════════
       SECTION 2: TRUST BAR
       ═══════════════════════════════════════════════════ -->
  <section class="py-12 bg-white border-b border-gray-100" aria-label="אמינות ואישורים">
    <div class="max-w-6xl mx-auto px-6">
      <p class="text-center text-sm text-navy-300 font-medium mb-8 tracking-wide">ניסיון בחברות מובילות</p>
      <div class="flex flex-wrap items-center justify-center gap-x-12 gap-y-6 opacity-40 grayscale">
        <!-- Placeholder logos -->
        {["חברה א׳", "חברה ב׳", "חברה ג׳", "חברה ד׳", "חברה ה׳"].map((name) => (
          <div class="flex items-center justify-center w-28 h-10 border border-gray-200 rounded text-xs text-gray-400 font-medium">
            {name}
          </div>
        ))}
      </div>
      <div class="flex flex-wrap items-center justify-center gap-4 mt-8">
        <span class="inline-flex items-center gap-1.5 px-3 py-1 bg-navy-50 rounded-full text-xs font-medium text-navy-400">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          APICS CSCP
        </span>
        <span class="inline-flex items-center gap-1.5 px-3 py-1 bg-navy-50 rounded-full text-xs font-medium text-navy-400">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          MBA
        </span>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════
       SECTION 3: PAIN POINTS / OVERVIEW
       ═══════════════════════════════════════════════════ -->
  <section class="py-24 bg-white scroll-mt-20">
    <div class="max-w-6xl mx-auto px-6">
      <div class="text-center max-w-2xl mx-auto mb-16">
        <h2 class="text-3xl sm:text-4xl font-extrabold text-navy-500 mb-4">האתגרים שמעכבים אתכם</h2>
        <p class="text-lg text-navy-300 leading-relaxed">
          רוב החברות מתמודדות עם אותם כשלים בשרשרת האספקה — ויש להם פתרון.
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        {painPoints.map((point, i) => (
          <div
            class="group relative p-8 rounded-2xl bg-gray-50/80 border border-gray-100 hover:border-teal-200 hover:shadow-lg hover:shadow-teal-500/5 transition-all duration-300"
          >
            <!-- Icon -->
            <div class="w-12 h-12 rounded-xl bg-navy-500 text-teal-400 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
              {point.icon === "cost" && (
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 17h8m0 0V9m0 8l-8-8-4 4-6-6"/></svg>
              )}
              {point.icon === "resilience" && (
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
              )}
              {point.icon === "visibility" && (
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
              )}
            </div>
            <h3 class="text-xl font-bold text-navy-500 mb-3">{point.title}</h3>
            <p class="text-navy-300 leading-relaxed">{point.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════
       SECTION 4: SERVICES
       ═══════════════════════════════════════════════════ -->
  <section id="services" class="py-24 bg-gray-50/50 scroll-mt-20">
    <div class="max-w-6xl mx-auto px-6">
      <div class="text-center max-w-2xl mx-auto mb-16">
        <span class="inline-block text-sm font-medium text-teal-600 mb-3 tracking-wide">מה אני מציעה</span>
        <h2 class="text-3xl sm:text-4xl font-extrabold text-navy-500 mb-4">שירותי ייעוץ</h2>
        <p class="text-lg text-navy-300 leading-relaxed">
          פתרונות מותאמים אישית לכל שלב בשרשרת האספקה — מאבחון ועד יישום.
        </p>
      </div>

      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {services.map((service) => (
          <div class="service-card group relative bg-white rounded-2xl border border-gray-100 hover:border-teal-200 overflow-hidden transition-all duration-300 hover:shadow-lg hover:shadow-teal-500/5">
            <div class="p-7">
              <h3 class="text-lg font-bold text-navy-500 mb-2 group-hover:text-teal-600 transition-colors">{service.title}</h3>
              <p class="text-sm text-navy-300 leading-relaxed mb-4">{service.brief}</p>
              <button
                class="service-toggle text-sm font-medium text-teal-600 hover:text-teal-500 transition-colors flex items-center gap-1"
                aria-expanded="false"
              >
                <span class="toggle-text">קראו עוד</span>
                <svg class="w-4 h-4 transition-transform duration-200 toggle-icon rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
              </button>
            </div>
            <div class="service-detail hidden px-7 pb-7">
              <div class="pt-4 border-t border-gray-100">
                <p class="text-sm text-navy-400 leading-relaxed">{service.detail}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════
       SECTION 5: METHODOLOGY
       ═══════════════════════════════════════════════════ -->
  <section id="methodology" class="py-24 bg-navy-500 scroll-mt-20">
    <div class="max-w-6xl mx-auto px-6">
      <div class="text-center max-w-2xl mx-auto mb-16">
        <span class="inline-block text-sm font-medium text-teal-400 mb-3 tracking-wide">איך אני עובדת</span>
        <h2 class="text-3xl sm:text-4xl font-extrabold text-white mb-4">תהליך עבודה מוכח</h2>
        <p class="text-lg text-navy-200 leading-relaxed">
          מתודולוגיה מובנית שמבטיחה תוצאות מדידות בכל פרויקט.
        </p>
      </div>

      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {methodology.map((phase, i) => (
          <div class="relative group">
            <div class="p-6 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-teal-400/30 transition-all duration-300 h-full">
              <span class="block text-4xl font-extrabold text-teal-400/30 mb-3 font-mono">{phase.step}</span>
              <h3 class="text-xl font-bold text-white mb-2">{phase.title}</h3>
              <p class="text-sm text-navy-200 leading-relaxed">{phase.desc}</p>
            </div>
            <!-- Connector arrow -->
            {i < 3 && (
              <div class="hidden lg:flex absolute top-1/2 -left-3 -translate-y-1/2 text-navy-400" aria-hidden="true">
                <svg class="w-6 h-6 rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 19l-7-7 7-7"/></svg>
              </div>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════
       SECTION 6: ABOUT
       ═══════════════════════════════════════════════════ -->
  <section id="about" class="py-24 bg-white scroll-mt-20">
    <div class="max-w-6xl mx-auto px-6">
      <div class="grid md:grid-cols-5 gap-12 items-center">
        <!-- Image / visual (40%) -->
        <div class="md:col-span-2 flex justify-center">
          <div class="relative w-full max-w-xs aspect-[3/4] rounded-2xl bg-gray-100 overflow-hidden">
            <div class="absolute inset-0 bg-gradient-to-t from-navy-500/20 to-transparent z-10"></div>
            <svg class="w-full h-full text-gray-200" viewBox="0 0 200 300" fill="currentColor" aria-hidden="true">
              <ellipse cx="100" cy="80" rx="50" ry="55" />
              <path d="M30 300 Q30 180 100 170 Q170 180 170 300 Z" />
            </svg>
            <div class="absolute bottom-4 left-4 right-4 z-20 text-center">
              <span class="text-xs text-gray-400 italic">מקום לתמונה</span>
            </div>
          </div>
        </div>

        <!-- Text (60%) -->
        <div class="md:col-span-3 space-y-6">
          <span class="inline-block text-sm font-medium text-teal-600 tracking-wide">הסיפור שלי</span>
          <h2 class="text-3xl sm:text-4xl font-extrabold text-navy-500">
            ניסיון קורפורטיבי,
            <br />
            גישה אישית.
          </h2>
          <div class="space-y-4 text-navy-300 leading-relaxed">
            <p>
              לאורך למעלה מ-15 שנות קריירה בתפקידי ניהול בכירים בשרשרת האספקה, צברתי ניסיון מעמיק בתעשיות מגוונות — מטכנולוגיה ותעשייה ועד קמעונאות ומזון.
            </p>
            <p>
              אני מאמינה שכל ארגון יכול להפוך את שרשרת האספקה שלו ממרכז עלות למנוע צמיחה. הגישה שלי משלבת חשיבה אסטרטגית עם יכולת יישום מעשית — כי תוכנית מצוינת שלא מיושמת היא רק מצגת יפה.
            </p>
          </div>
          <div class="grid grid-cols-2 gap-6 pt-4">
            <div class="p-4 rounded-xl bg-gray-50">
              <span class="block text-sm font-bold text-navy-500 mb-1">התמחויות</span>
              <p class="text-xs text-navy-300 leading-relaxed">אופטימיזציה, S&OP, טרנספורמציה דיגיטלית, רכש אסטרטגי</p>
            </div>
            <div class="p-4 rounded-xl bg-gray-50">
              <span class="block text-sm font-bold text-navy-500 mb-1">ערכים מנחים</span>
              <p class="text-xs text-navy-300 leading-relaxed">שקיפות, מבוסס נתונים, שותפות אמיתית, תוצאות מדידות</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════
       SECTION 7: CONTACT
       ═══════════════════════════════════════════════════ -->
  <section id="contact" class="py-24 bg-gray-50/50 scroll-mt-20">
    <div class="max-w-6xl mx-auto px-6">
      <div class="grid md:grid-cols-2 gap-16 items-start">

        <!-- Contact info -->
        <div class="space-y-8">
          <div>
            <span class="inline-block text-sm font-medium text-teal-600 tracking-wide mb-3">צרו קשר</span>
            <h2 class="text-3xl sm:text-4xl font-extrabold text-navy-500 mb-4">
              בואו נדבר על
              <br />
              שרשרת האספקה שלכם
            </h2>
            <p class="text-lg text-navy-300 leading-relaxed">
              מלאו את הפרטים ואחזור אליכם תוך יום עסקים אחד לשיחת היכרות ללא התחייבות.
            </p>
          </div>

          <div class="space-y-4">
            <h3 class="text-lg font-bold text-navy-500">דרכים נוספות ליצירת קשר</h3>
            <div class="space-y-3">
              <a href="mailto:yasmin@example.com" class="flex items-center gap-3 text-navy-300 hover:text-teal-600 transition-colors">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
                <span class="text-sm">yasmin@example.com</span>
              </a>
              <a href="https://www.linkedin.com/" target="_blank" rel="noopener noreferrer" class="flex items-center gap-3 text-navy-300 hover:text-teal-600 transition-colors">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
                <span class="text-sm">LinkedIn</span>
              </a>
            </div>
          </div>
        </div>

        <!-- Contact Form -->
        <div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-8">
          <h3 class="text-2xl font-bold text-navy-500 mb-2">בואו נדבר</h3>
          <p class="text-sm text-navy-300 mb-8">מלאו את הפרטים ואחזור אליכם תוך יום עסקים אחד.</p>

          <form class="space-y-5" action="#" method="POST">
            <div>
              <label for="name" class="block text-sm font-medium text-navy-500 mb-1.5">שם מלא</label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 rounded-lg border border-gray-200 bg-gray-50/50 text-navy-500 placeholder-navy-200 text-sm focus:outline-none focus:ring-2 focus:ring-teal-500/30 focus:border-teal-500 transition-all"
                placeholder="ישראל ישראלי"
              />
            </div>

            <div>
              <label for="email" class="block text-sm font-medium text-navy-500 mb-1.5">אימייל</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 rounded-lg border border-gray-200 bg-gray-50/50 text-navy-500 placeholder-navy-200 text-sm focus:outline-none focus:ring-2 focus:ring-teal-500/30 focus:border-teal-500 transition-all"
                placeholder="israel@company.co.il"
              />
            </div>

            <div>
              <label for="industry" class="block text-sm font-medium text-navy-500 mb-1.5">תעשייה</label>
              <select
                id="industry"
                name="industry"
                class="w-full px-4 py-3 rounded-lg border border-gray-200 bg-gray-50/50 text-navy-500 text-sm focus:outline-none focus:ring-2 focus:ring-teal-500/30 focus:border-teal-500 transition-all appearance-none"
              >
                <option value="">בחרו תעשייה</option>
                <option value="tech">טכנולוגיה</option>
                <option value="manufacturing">תעשייה ויצור</option>
                <option value="retail">קמעונאות</option>
                <option value="food">מזון ומשקאות</option>
                <option value="pharma">פארמה ורפואה</option>
                <option value="logistics">לוגיסטיקה ושינוע</option>
                <option value="other">אחר</option>
              </select>
            </div>

            <div>
              <label for="challenge" class="block text-sm font-medium text-navy-500 mb-1.5">האתגר המרכזי שלכם</label>
              <textarea
                id="challenge"
                name="challenge"
                rows="4"
                class="w-full px-4 py-3 rounded-lg border border-gray-200 bg-gray-50/50 text-navy-500 placeholder-navy-200 text-sm focus:outline-none focus:ring-2 focus:ring-teal-500/30 focus:border-teal-500 transition-all resize-none"
                placeholder="ספרו לי בקצרה על האתגר המרכזי שאתם מתמודדים איתו בשרשרת האספקה..."
              ></textarea>
            </div>

            <button
              type="submit"
              class="w-full py-4 px-6 bg-teal-500 text-navy-500 font-bold rounded-lg text-base hover:bg-teal-400 transition-all duration-200 hover:shadow-lg hover:shadow-teal-500/20 hover:-translate-y-0.5 focus:outline-none focus:ring-2 focus:ring-teal-500/50 focus:ring-offset-2"
            >
              שליחה
            </button>

            <p class="text-xs text-navy-200 text-center">אני מכבדת את הפרטיות שלכם. המידע לא יועבר לצד שלישי.</p>
          </form>
        </div>

      </div>
    </div>
  </section>

</BaseLayout>

<script>
  // Service card accordion
  document.querySelectorAll(".service-toggle").forEach((btn) => {
    btn.addEventListener("click", () => {
      const card = btn.closest(".service-card");
      const detail = card?.querySelector(".service-detail");
      const icon = btn.querySelector(".toggle-icon");
      const text = btn.querySelector(".toggle-text");

      if (!detail || !icon || !text) return;

      const isOpen = btn.getAttribute("aria-expanded") === "true";
      btn.setAttribute("aria-expanded", String(!isOpen));
      detail.classList.toggle("hidden");
      icon.classList.toggle("rotate-90");
      icon.classList.toggle("rotate-180");
      text.textContent = isOpen ? "קראו עוד" : "סגירה";
    });
  });

  // Scroll-triggered animations
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("animate-fade-up");
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.1, rootMargin: "0px 0px -50px 0px" }
  );

  document.querySelectorAll("[data-animate]").forEach((el) => {
    observer.observe(el);
  });

  // Smooth scroll for anchor links
  document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener("click", (e) => {
      const href = anchor.getAttribute("href");
      if (!href || href === "#") return;
      const target = document.querySelector(href);
      if (target) {
        e.preventDefault();
        target.scrollIntoView({ behavior: "smooth" });
      }
    });
  });
</script>
